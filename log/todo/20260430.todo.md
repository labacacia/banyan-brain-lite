## [P0 — Solution Scaffold] ✓ COMPLETE
- [x] Create directory structure (src/, tests/, docs/architecture/, log/todo/)
- [x] Write Directory.Build.props + nuget.config (NPS private feed: nexus.ts01.starfield.com.au)
- [x] Scaffold Banyan.sln + 8 projects
- [x] Wire project references
- [x] Domain types in Banyan.Core (Memory, IMemoryStore, IEmbedder, request/response models)
- [x] INipCaProvider + NipCaProviderFactory + EmbeddedMiniCaProvider + ExternalCaProvider stubs in Banyan.Auth
- [x] Stubs in Banyan.Lite, Banyan.Node, Banyan.Embedders, Banyan.Cli
- [x] Smoke tests in Banyan.Core.Tests (5) + Banyan.Lite.Tests (1) — xunit.v3 1.1.0 + runner.visualstudio 3.x
- [x] dotnet test green (6/6 passed)

## [P1 — Minimal Storage (BM25 only)] ✓ COMPLETE
- [x] SQLite schema + migrations (memory_events, memories_current, FTS5)
- [x] SqliteMemoryStore: Write / Get / Search(BM25) / Trace
- [x] Integration tests: 14/14 green (prefix matching fixed for plural tokens)

## [P1.5 — Identity Layer (dual-track)]

### P1.5a — NID track (Agent ↔ Node, Banyan.Auth)
- [x] Drop the home-grown `INipCaProvider` / `NidCertificate` stubs — Banyan now uses NPS.NIP types directly
- [x] `SqliteNipCaStore` (`INipCaStore` impl) + migrations on independent `nipca.db` (no PG dependency)
- [x] `EmbeddedNipCa` facade wrapping `NipCaService` + `NipKeyManager` (Ed25519 with passphrase-encrypted PEM)
- [x] `BanyanNipCaOptions` + `BanyanMemoryNode` ctor takes `EmbeddedNipCa`
- [x] `Banyan.Auth.Tests` — 20/20 covering store CRUD + end-to-end Register/Verify/Revoke
- [x] CLI: `banyan ca init / info`, `banyan agent issue / list / revoke / verify`
- [ ] `RemoteNipCaClient` HTTP client to talk to a remote NPS-CA server (deferred to P3 with `banyan serve`)

### P1.5b — OLS track (Operators / admins, Banyan.Identity, NEW)
- [x] nuget.config: env-var creds for nexus-lc01 + OLS.* package source mapping
- [x] Reflect OLS.Root.Core/Authentication/Authorisation/Oidc DLLs → docs/architecture/ols-surface-reference.{md,cn.md}
- [x] Architecture decisions locked → docs/architecture/identity.{md,cn.md}
- [x] User exports NEXUS_USERNAME / NEXUS_PASSWORD; verify dotnet restore can pull OLS.Root.* from Nexus
- [x] Create src/Banyan.Identity project + add to Banyan.slnx
- [x] BanyanIdentityOptions + IdentityMigrations (schema 001 — 10 tables)
- [x] SqliteUserStore (IUserStore + IUserPasswordStore + IUserEmailStore + IUserLockoutStore + IUserRoleStore + IUserTwoFactorStore)
- [x] SqliteRoleStore
- [x] SqliteRefreshTokenStore
- [x] SqliteOidcClientStore (with UpsertAsync for `banyan init` seed) + SqliteAuthorizationCodeStore + SqliteDeviceCodeStore + SqliteReferenceTokenStore
- [x] SqliteIdentityStore facade (mirrors SqliteMemoryStore.OpenAsync style)
- [x] tests/Banyan.Identity.Tests: 33/33 green covering all 7 stores
- [x] PemSigningKeyLoader (RSA PEM → SecurityKey + SigningCredentials), 7 unit tests
- [x] AddBanyanIdentity DI extension wiring AddOlsIdentityCore + AddOlsAuthentication + AddOlsAuthorisation + AddOlsOidc, 3 DI tests
- [x] Banyan.Cli commands: keygen / init / login (Device Code) / whoami / logout
- [ ] Banyan.Cli login --browser (Authorization Code + PKCE — deferred to P3 along with `banyan serve`)

## [P2 — Vector + Hybrid Retrieval] ✓ COMPLETE
- [x] `embeddings` table (Migration 002) — float32 BLOB, model_id + dim, FK CASCADE on memories_current
- [x] `HashingEmbedder` (FastText-style char 3–5 gram FNV → 384-dim, deterministic, offline, zero-deps) — replaces PlaceholderEmbedder
- [x] `SqliteMemoryStore.OpenAsync(connStr, embedder?)` — Write/Update transactionally write embedding row
- [x] `LexicalSearchAsync` (BM25, existing) + `VectorSearchAsync` (cosine over normalised vectors, linear scan + top-K) + `HybridSearchAsync` (RRF k=60, fuses pool of `max(K*4, 50)` per ranker)
- [x] `UpdateAsync` — append Update event, refresh memories_current + FTS row + embedding via UPSERT
- [x] `ForgetAsync` — append Tombstone event with reason, drop from search/embedding (trace preserved for audit)
- [x] `Banyan.Web`: `/api/memory/search?mode=hybrid|lexical|vector`, `PUT /{id}` update, `DELETE /{id}?reason=…` forget
- [x] Web UI: 3-button segmented mode control, lex/vec rank badges, edit + forget actions on memory detail
- [x] Tests: 13 new (HashingEmbedder 6, Hybrid 4, Update/Forget 6) → Lite 31/31

## [P2.1 — Real semantic embedder] ✓ COMPLETE
- [x] `Banyan.Embedders` adds `Microsoft.ML.OnnxRuntime` + `Microsoft.ML.Tokenizers` packages
- [x] `OnnxEmbedder` (`IEmbedder`) — BERT WordPiece + ONNX inference + mean-pool + L2-norm; default model `bge-small-zh-v1.5` (Xenova INT8, 22MB ONNX + 106KB vocab.txt, 384-dim, multilingual-leaning)
- [x] `OnnxEmbedderOptions` — model path / vocab path / max seq len / model_id / BGE retrieval-query prefix
- [x] `IEmbedder.EmbedQueryAsync` default-method (default = passage); `OnnxEmbedder` overrides with the BGE query prefix; `SqliteMemoryStore.VectorSearchAsync` calls it
- [x] `EmbedderFactory.Create()` — env-vars `BANYAN_EMBEDDER` / `_MODEL` / `_VOCAB`, auto-fallback to `HashingEmbedder` when ONNX assets missing
- [x] `Banyan.Cli`: `banyan embedder download` (HuggingFace pull with progress), `banyan embedder info` (load + sample embedding)
- [x] `Banyan.Web`: `WebApp.RunAsync` calls `EmbedderFactory.Create()`; agent endpoints only mount when CA is loaded
- [x] Tests: 6 ONNX integration tests (auto-skip if model missing) — Lite 37/37
- [x] Smoke: bilingual semantic hits — `lost my keys` → `I left my keys at the office`; `周二会议` → `团队会议每周二下午两点`

## [P2.2 — sqlite-vec ANN] ✓ COMPLETE
- [x] `SqliteVecLoader` — cross-platform path discovery (explicit → env `BANYAN_SQLITE_VEC_LIB` → `~/.banyan/sqlite-vec/{vec0.so|.dll|.dylib}` → `runtimes/{rid}/native/`), `EnableExtensions` + `LoadExtension`, `vec_version()` smoke probe
- [x] `embeddings_vec USING vec0(memory_id TEXT PRIMARY KEY, embedding float[N])` ANN sidecar (BLOB column remains source of truth for backfill / migration safety)
- [x] `SqliteMemoryStore.OpenAsync(connStr, embedder, sqliteVecLibPath?)` — auto-creates the vec0 table + backfills from `embeddings` if it loads; `VecEnabled` property exposes status
- [x] Write / Update / Forget transactionally double-write to `embeddings_vec` (vec0 has no UPSERT — delete-then-insert per row on Update)
- [x] `VectorSearchAsync` — vec0 KNN (`MATCH … AND k = ?`) with namespace post-filter; cosine = `1 - distance/2` (vec0 default L2²); falls back to linear scan when extension unavailable
- [x] CLI `banyan embedder download` extended: also pulls sqlite-vec **v0.1.9** loadable extension (~60 KB tar.gz) for the running OS+arch via `System.Formats.Tar`; `--no-vec` opts out
- [x] `Banyan.Web` `WebOptions.SqliteVecLibPath`, `--vec-lib` flag on `banyan web`, startup banner `[store] sqlite-vec ANN index ready`
- [x] Tests: 5 SqliteVecTests (auto-skip if vec0 absent) — Lite 42/42, full solution **110/110**
- [x] Smoke: ANN end-to-end — same bilingual semantic hits as linear scan path, scores within float drift

## [P1.6 — Demo Web UI] ✓ COMPLETE
- [x] `Banyan.Web` project (ASP.NET Core minimal APIs, net10.0)
- [x] `/api/memory` write / search / get / trace
- [x] `/api/agents` issue / list / revoke / verify
- [x] `/api/ca` info · `/api/identity/me` whoami · `/api/health`
- [x] SPA wwwroot: index.html + style.css + app.js (3 tabs: Memory · Agents · About)
- [x] Visual style: ink navy + electric blue + hot pink, glassmorphism cards, gradient brand text, neon status pills, canvas particle network with cursor pull
- [x] `banyan web` CLI subcommand; wwwroot copied to CLI bin via Content/Update
- [x] Smoke: GET /, /api/health, write/search/issue/revoke/verify all 200

## [P3 — NPS Memory Node] ✓ COMPLETE
- [x] Bumped LabAcacia.NPS.{Core,NIP,NWP} to **1.0.0-alpha.5** — NWP now ships `MemoryNodeMiddleware` + `IMemoryNodeProvider` + `NeuralWebManifest` + `NptMeter`, and NIP adds `NPS.NIP.X509` (cert-form NID) — wholesale replacing my hand-rolled HTTP shim
- [x] `Banyan.Node` re-skinned as a Web SDK project hosting the standard NWP pipeline
- [x] `BanyanNodeOptions` — Banyan-side wrapper (paths, urls, trusted issuers, NodeId/DisplayName, RequireAuth)
- [x] `BanyanMemoryProvider : IMemoryNodeProvider` — translates `QueryFrame.Filter`/`VectorSearch`/`Limit`/`Fields` into Banyan's `SqliteMemoryStore.SearchAsync` (BM25 / vector / hybrid via RRF), surfaces `lex_rank`/`vec_rank`/`score` per row, exposes `BuildSchema()` (memory_id, namespace, content, agent_nid, created_at, updated_at, score, lex_rank, vec_rank)
- [x] `MemoryNodeApp.RunAsync` — wires `services.AddNwp` + `services.AddMemoryNode<BanyanMemoryProvider>` + `app.UseMemoryNode<BanyanMemoryProvider>` (auto-mounts `/api/memory/query` and `/api/memory/stream` via NWP middleware), plus `NipIdentVerifier` + `NipVerifierOptions` for IdentFrame validation
- [x] `/.nwm` — full `NeuralWebManifest` (NodeAuth, NodeCapabilities, NodeEndpoints, Graph), declares ed25519/NIP auth + vector search + token-budget hints
- [x] `/.schema` — exposes `MemoryNodeSchema` (NWP middleware claims `/api/memory/*` so schema lives outside that prefix; manifest points clients there)
- [x] `banyan serve` CLI subcommand (replaces old stub) — flags: `--urls`, `--memory-db`, `--vec-lib`, `--ca-{db,key,nid}`, `--node-id`, `--allow-anon`, repeatable `--trusted-issuer NID=PUB`
- [x] `Banyan.Node.Tests` — 8 tests on `BanyanMemoryProvider` (text filter, field projection, limits, schema fields, stream batching, count)
- [x] Full regression **118/118** (Lite 42 + Auth 20 + Identity 43 + Core 5 + Node 8)
- [x] Smoke (against Web-seeded `memory.db`, mixed CN/EN ONNX corpus):
        `GET /api/health` → `{ok,role=memory-node,version=P3}`
        `GET /.nwm` → full NeuralWebManifest with capabilities + endpoints
        `GET /.schema` → 9-field MemoryNodeSchema
        `POST /api/memory/query` `{filter:{text:"deadline"}, limit:5}` → wire-format frame `{frame_type, preferred_tier, anchor_ref:sha256:..., count, data[], next_cursor, token_est}` with hybrid lex+vec hits including the bilingual semantic match `team meets Tuesday afternoons` ↔ `团队会议每周二下午两点`

## [P3.1 — Remote NIP CA] ✓ COMPLETE
- [x] Spike confirmed: alpha.4 + alpha.5 NPS.NIP `MapNipCa` register **0 endpoints** — the .NET package's HTTP routing is still stubbed. Reference impl lives at [github.com/labacacia/nip-ca-server](https://github.com/labacacia/nip-ca-server) (Go is the working port; .NET csproj exists but routes-less)
- [x] **Banyan fills the gap**: hand-written NPS-3 §8 conformant routes in `Banyan.Web/Endpoints/NipCaEndpoints.cs`, mirroring the Go reference verbatim:
        `POST /v1/{agents,nodes}/register`, `POST /v1/agents/{nid}/{renew,revoke}`,
        `GET  /v1/agents/{nid}/verify`, `GET /v1/ca/cert`, `GET /v1/crl`,
        `GET /.well-known/nps-ca`, `GET /health`
- [x] Mounted whenever the host loads an `EmbeddedNipCa` — both `banyan web` (Banyan.Web/WebApp) and `banyan serve` (Banyan.Node/MemoryNodeApp) become NPS-CA conformant out of the box
- [x] `Banyan.Auth/RemoteNipCaClient` — `HttpClient`-based client mirroring `EmbeddedNipCa`'s API surface; explicit snake_case dictionary bodies, `[JsonPropertyName]` request records, snake-case response naming policy. `RemoteNipCaException` carries upstream status + body
- [x] CLI: `banyan agent issue|verify|revoke` accept `--remote URL` (or env `BANYAN_CA_URL`), routing through `RemoteNipCaClient` instead of `EmbeddedNipCa`. `agent list` stays local (NPS spec doesn't expose list)
- [x] `Banyan.Auth.Tests` adds 7 round-trip tests against an in-process `WebApplication` host on an ephemeral port — Auth 27/27 (was 20)
- [x] Smoke (Web demo acting as CA, CLI as remote client): issue (serial 4) → verify VALID → revoke (`cli-cleanup`) → verify INVALID/`NIP-CERT-REVOKED`

## [P4 — Packaging] ✓ COMPLETE
- [x] **Shared metadata in `Directory.Build.props`**: Version `0.1.0-alpha.1`, Apache-2.0, INNO LOTUS, repo URL, package tags, snupkg symbols, repo-root README auto-included in every packable project
- [x] **5 NuGet packages**: `Banyan.Core` (14KB), `Banyan.Lite` (28KB), `Banyan.Embedders` (12KB), `Banyan.Auth` (29KB), `Banyan.Cli` (135MB — bundles ASP.NET Core, OnnxRuntime, OLS, NPS dependencies via `--self-contained` style packing)
- [x] **dotnet tool**: `Banyan.Cli` packs as `PackAsTool=true`, `ToolCommandName=banyan`. Verified: `dotnet tool install -g Banyan.Cli` ⇒ `banyan --help` works
- [x] `README.md` + `README.cn.md` — top-level intro, features, quick-start (8 commands), project structure, doc index, dependency credits, license
- [x] `docs/architecture/storage-tiers.{md,cn.md}` — three-DB model (`memory.db` / `identity.db` / `nipca.db`), full table layouts, lifecycle table for Write/Update/Forget across `memory_events` + `memories_current` + FTS5 + `embeddings` + `embeddings_vec`, threat model
- [x] `docs/architecture/nps-mapping.{md,cn.md}` — NCP / NWP / NIP layer-by-layer adoption status, `BanyanMemoryProvider` filter DSL, full `banyan serve` route table, NIP CA spike documentation, frame inventory, "what's not mapped yet"
- [x] Full regression: **Lite 42 + Auth 27 + Identity 43 + Core 5 + Node 8 = 125/125**
